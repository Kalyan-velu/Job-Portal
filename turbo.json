{
  "$schema": "https://turborepo.com/schema.json",
  "globalEnv": ["NODE_ENV"],
  "tasks": {
    "build": {
      "env": [
        "MONGO_URL",
        "JWT_SECRET",
        "EMAIL_USER",
        "EMAIL_PASS",
        "BASE_URL",
        "NODE_ENV",
        "PORT",
        "EMAIL_CLIENT_ID",
        "EMAIL_CLIENT_SECRET",
        "EMAIL_REFRESH_TOKEN",
        "EMAIL_ACCESS_TOKEN"
      ],
      "dependsOn": ["^build"],
      "inputs": [
        "$TURBO_DEFAULT$",
        ".env.production.local",
        ".env.local",
        ".env.production",
        ".env"
      ],
      "outputs": ["dist/**"]
    },
    "dev": {
      "inputs": [
        "$TURBO_DEFAULT$",
        ".env.development.local",
        ".env.local",
        ".env.development",
        ".env"
      ],
      "persistent": true,
      "cache": false
    },
    "start": {
      "dependsOn": ["build"],
      "env": [
        "MONGO_URL",
        "JWT_SECRET",
        "EMAIL_USER",
        "EMAIL_PASS",
        "BASE_URL",
        "NODE_ENV",
        "PORT",
        "EMAIL_CLIENT_ID",
        "EMAIL_CLIENT_SECRET",
        "EMAIL_REFRESH_TOKEN",
        "EMAIL_ACCESS_TOKEN"
      ],
      "inputs": [
        "$TURBO_DEFAULT$",
        ".env.development.local",
        ".env.local",
        ".env.development",
        ".env"
      ]
    }
  }
}
